import java.util.Scanner;

public class Apart4 {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        // loop
        for (double counter = 10; counter >= 0; counter -= 2) {
            System.out.println(counter);
        }

    }
}
